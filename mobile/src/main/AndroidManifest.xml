<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="org.dash.avionics"
          android:versionCode="1"
          android:versionName="1.0">

  <uses-sdk
      android:minSdkVersion="18"
      android:targetSdkVersion="20"/>

  <uses-permission android:name="android.permission.BLUETOOTH"/>

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:theme="@style/AppTheme">
    <activity
        android:name="org.dash.avionics.AvionicsActivity_"
        android:configChanges="orientation|keyboardHidden|screenSize"
        android:immersive="true"
        android:label="@string/app_name"
        android:screenOrientation="landscape"
        android:theme="@style/FullscreenTheme">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>

        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity
        android:name="org.dash.avionics.PropCalibrationActivity_"
        android:configChanges="orientation|keyboardHidden|screenSize"
        android:immersive="true"
        android:label="@string/app_name"
        android:screenOrientation="landscape"
        android:theme="@style/FullscreenTheme"/>

    <service android:name=".sensors.SensorsService_"/>

    <provider
        android:name=".data.MeasurementContentProvider_"
        android:authorities="org.dash.avionics"
        android:exported="false"/>
  </application>

</manifest>